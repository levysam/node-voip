import { BaseRepository } from '#src/repositories/BaseRepository.js';

class CallDetailRepository extends BaseRepository {
  constructor() {
    super('call');
  }
}

export default CallDetailRepository;
